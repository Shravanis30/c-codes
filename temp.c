// change the tempreature from fahrenheit to celsius
#include<stdio.h>
int main() {
    int fahrenheit, c;
    printf("Enter the temperature in Fahrenheit: ");
    scanf("%f", &fahrenheit);
    
    printf("Temperature in celsius: %f",c = (5/9) * (fahrenheit -32));
    return 0;
}
